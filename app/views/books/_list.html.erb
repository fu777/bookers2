
<div>
  <h1>Books</h1>
  <h4>title</h4>
  <h4>opinion</h4>
<% books.each do |book| %>
  <%= image_tag book.user.get_profile_image(40, 40) %>
  <%= link_to book_path(book.id) do %>
  <p><%= book.title %></p>
  <% end %>
  <p><%= book.body %></p>
<% end %>
</div>

